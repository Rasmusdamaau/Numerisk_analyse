n = 1;
while abs(gamma(n + 1) - factorial(n)) < eps
    n = n +1;
end
n

%%
gamma(n + 1) - factorial(n)
gamma(n + 1)
factorial(n)
%%
n = 1;
while gamma(n + 1) ~= inf
    n = n + 1;
end
n

n = 1;
while factoria

factorial(n);
factorial(n) == inf;

% 1,7977e + 308, største mulige tal
realmax;

